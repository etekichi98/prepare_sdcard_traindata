# train and val datasets (image directory or *.txt file with image paths)
train: training/train/images
val: training/test/images

# number of classes
nc: 2

# class names
names: ['sd-front', 'sd-back']
